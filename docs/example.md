# Example Doc

This is a sample Markdown file. Add or edit files in `docs/` and refresh the
browser — no restart or build step needed.

## Code sample

```js
function hello(name) {
    return `Hello, ${name}!`;
}
```

## List

- Renders via `marked`
- Sanitized via `DOMPurify`
- Code highlighted via `highlight.js`

What if i edit this file while server is running.
